assets:
  overrides:
    /: ../frontendquiz/

server:
  rootPath: '/api/*'

database:
  driverClass: org.sqlite.JDBC
  user: exam
  password: notes!
  url: jdbc:sqlite:notes.db
  properties:
    charSet: UTF-8